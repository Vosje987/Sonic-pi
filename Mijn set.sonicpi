#-- Sonic Pi Set v1
#-- meta {"current":0,"zooms":[-4,-2,-1,2,2,2,2,2,2,2]}
#-- buffer 0
#Main
#-- buffer 1
#Possible synths and samples
use_synth :piano
sample :ambi_choir
sample :drum_heavy_kick #also other drum samples
use_synth :sc808_bassdrum
use_synth :dark_ambience
sample :loop_amen
#-- buffer 2
#Testing --> not the final song

live_loop :test do
  use_synth :piano
  
  use_random_seed 30
  
  16.times do
    sample :loop_amen, onset: pick
    sleep 0.125
  end
  
  
  
end
